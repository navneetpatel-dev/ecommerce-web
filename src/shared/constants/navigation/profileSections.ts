/**
 * Default profile tab per surface. Shared because both the customer account
 * feature and role-aware profile path helpers (shared/utils/profilePaths)
 * depend on them.
 */
export const DEFAULT_ACCOUNT_SECTION = "overview";
export const DEFAULT_WORKSPACE_ACCOUNT_SECTION = "personal";
