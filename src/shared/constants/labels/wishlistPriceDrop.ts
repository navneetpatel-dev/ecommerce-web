/**
 * Wishlist price-drop badge copy. NOT yet merged into the root `labels.ts`
 * — see "SHARED FILE CHANGES NEEDED" in the task report; import directly
 * from this module until then.
 */
export const wishlistPriceDropLabels = {
  wishlistPriceDropped: "Price dropped",
  wishlistPriceDropSaved: "You save {amount}",
} as const;
