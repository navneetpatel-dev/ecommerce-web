/**
 * Debounce window for high-frequency filter inputs (price min/max) so typing
 * does not fire a navigation per keystroke (performance standard §6).
 */
export const FILTER_INPUT_DEBOUNCE_MS = 350;
