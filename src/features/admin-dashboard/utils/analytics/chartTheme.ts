/** Fixed color-role rotation for admin status charts (order/return status breakdowns). */
export const STATUS_CHART_PALETTE = [
  "brand",
  "success",
  "warning",
  "danger",
  "inkMuted",
  "inkFaint",
] as const;
