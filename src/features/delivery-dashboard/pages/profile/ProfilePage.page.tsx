"use client";

import { AccountPage } from "@/features/account";

/**
 * Standard workspace account and settings page (Personal / Field Operations / Security / Privacy),
 * completely uniform with Admin and Vendor modules.
 */
export function DeliveryProfilePage() {
  return <AccountPage />;
}
